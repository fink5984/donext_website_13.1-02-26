// This file is not used - actual layout is in [locale]/layout.js
// Required by Next.js but empty because we use [locale] segment
export default function RootLayout({ children }) {
  return children;
}
